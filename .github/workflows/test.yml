name: Playwright Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    timeout-minutes: 10
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 18
      - name: Install dependencies (root)
        run: npm ci
      - name: Install backend deps
        run: cd backend && npm ci
      - name: Install frontend deps
        run: cd frontend && npm ci
      - name: Install Playwright browsers
        run: npx playwright install --with-deps
      - name: Run Playwright tests with verbose output
        run: |
          npx playwright test --reporter=html,list
        continue-on-error: true  # Don't stop even if tests fail
      - name: Debug - list all files
        if: always()
        run: |
          echo "Current directory:"
          pwd
          echo "Files in root:"
          ls -la
          echo "Looking for playwright-report:"
          ls -la playwright-report/ 2>/dev/null || echo "playwright-report folder NOT found"
          echo "Looking for test-results:"
          ls -la test-results/ 2>/dev/null || echo "test-results folder NOT found"
      - name: Upload Playwright report
        uses: actions/upload-artifact@v5
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
          if-no-files-found: warn